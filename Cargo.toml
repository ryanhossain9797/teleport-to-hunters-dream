[package]
name = "lantern-teleport"
version = "0.1.0"
edition = "2024"
description = "A minimal CLI tool to teleport to Hunter's Dream in Bloodborne save files"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
